apiVersion: rbac.authorization.k8s.io/v1beta1
kind: Role
metadata:
  labels:
    app: fluentd
  name: monitoring-fluentd
rules:
- apiGroups:
  - extensions
  resourceNames:
  - monitoring-fluentd
  resources:
  - podsecuritypolicies
  verbs:
  - use
