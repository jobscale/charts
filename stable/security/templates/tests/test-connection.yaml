apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "security.fullname" . }}-test-connection"
  labels:
{{ include "security.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "security.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
